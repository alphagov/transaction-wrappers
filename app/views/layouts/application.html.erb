<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title><%= yield :title %> - GOV.UK</title>
    <%= yield :head %>
  </head>

  <body class="mainstream <%= yield :body_classes %>">
    <div id="wrapper">
      <%= yield %>
    </div>
  </body>
</html>
